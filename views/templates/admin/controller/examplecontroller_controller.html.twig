{% extends '@PrestaShop/Admin/layout.html.twig' %}
{% set layoutTitle = 'Test Admin Controller'|trans({}, 'Modules.MyModule.Admin') %}

{% block content %}
    <div class="row justify-content-center">
        <div class="col">
            <div class="card">
                <div class="card-header">
                    {{ 'Test display data'|trans({}, 'Modules.MyModule.Admin') }}
                </div>
                <div class="card-body">
                  <h1>Hello World</h1>
                  <h3>Prestashop version is: {{ versionInstalled }}</h3>
                  <h3>Youe employee language is: {{ languageCode }}</h3>
                </div>

            </div>
        </div>
    </div>

{% endblock %}
